name: CI/CD

on:
  pull_request:
    types: [opened, edited, synchronize]

jobs:
  validate-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Validate PR title
        run: |
          TITLE="${{ github.event.pull_request.title }}"
          if ! echo "$TITLE" | grep -qE '^(feat|fix|docs|chore|test|refactor|ci)(\(.+\))?!?: .+'; then
            echo "❌ PR title must follow Conventional Commits: type(scope)!: description"
            echo "Allowed types: feat, fix, docs, chore, test, refactor, ci"
            exit 1
          fi

      - name: Validate PR description
        run: |
          if ! grep -qE ".{20,}" <<< "${{ github.event.pull_request.body }}"; then
            echo "❌ PR description too short. Please provide context."
            exit 1
          fi

  build-and-deploy:
    needs: validate-pr
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Build frontend
        run: npm --workspace frontend run build
      - name: Build backend
        run: npm --workspace backend run build
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: wasm32-unknown-unknown
      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "contract"
      - name: Build contract
        run: cargo build --locked --target wasm32-unknown-unknown --release
        working-directory: contract